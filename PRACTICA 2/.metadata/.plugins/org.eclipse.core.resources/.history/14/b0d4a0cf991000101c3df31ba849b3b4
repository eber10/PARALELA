program if2b;
var
	numero:integer;
begin
	writeln('Escriba un numero');
	readln(numero);
	if numero < 0 then
		writeln('El numero es negativo.')
	if numero>= 0 then
		writeln('El numero es positivo o cero.')
end.
